services:
  intaris:
    build: .
    ports:
      - "8060:8060"
    environment:
      - LLM_API_KEY=${LLM_API_KEY:-${OPENAI_API_KEY}}
    volumes:
      - intaris-data:/data
    restart: unless-stopped

volumes:
  intaris-data:
